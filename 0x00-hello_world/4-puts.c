#include <stdio.h>

/**
 *main - Entry Point
 *@message - array
 *
 * Description: prints a string
 * Return: 0 (Successful)
 */
int main(void)
{
	char message[60] = "\"Programming is like building a multilingual puzzle";

	puts(message);
	return (0);
}
